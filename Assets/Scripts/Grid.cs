using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Tilemaps;

public class Grid : MonoBehaviour
{
    /*
    public Tilemap tilemap;
    public GameObject cell;

    public void Start()
    {

    tilemap = GetComponent<Tilemap>();
    Vector3Int ori = tilemap.origin;
    Debug.Log(ori);

    GridLayout gridLayout = GetComponent<GridLayout>();
    Vector3Int cellPosition = gridLayout.WorldToCell(ori);
    Debug.Log(cellPosition);
    //Debug.Log(gridLayout.CellToWorld(cellPosition));

    //Sprite sprite = tilemap.GetTile(ori).sprite;
    //Sprite sprite = GetSprite(ori);
    SpriteRenderer spriteR = GameObject.Find().GetComponent<SpriteRenderer>();
    spriteR.color = Color.red;

    //tilemap.SetTileFlags(ori, TileFlags.None);
    //tilemap.SetColor(ori, Color.red);

    }

    */
}
