using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Testing : MonoBehaviour
{
/*
    public HexGrid grid;
    void Start()
    {
        //DamagePopup.Create(Vector3.zero, 300);
    }

    void Update() {
        if(Input.GetMouseButton(0)) {
            Vector3 mouse = grid.GetCell(Camera.main.ScreenPointToRay(Input.mousePosition)).Position;
            //Vector3 hexPos = new Vector3(HexCoordinates.FromPosition(mouse).X,HexCoordinates.FromPosition(mouse).Y);
            Vector3 hexPos = HexCoordinates.ToVector3(HexCoordinates.FromPosition(mouse));
            DamagePopup.Create(hexPos, 300);
        }
        if(Input.GetKeyDown(KeyCode.Alpha0)){
            foreach (HexUnit unit in grid.GetUnits()) {
                Vector3 pos = unit.transform.localPosition;
                Vector3 hexPos = new Vector3(HexCoordinates.FromPosition(pos).X,
                HexCoordinates.FromPosition(pos).Y);
                //Location //cell
                DamagePopup.Create(hexPos, unit.health);
            }
        }
    }


*/
}
